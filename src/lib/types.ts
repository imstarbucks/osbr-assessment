export type NavListType = {
  label: string;
  href: string;
};

export type ServiceType = {
  iconUrl: string;
  name: string;
  japaneseName: string;
};

export type CursorSize = "none" | "big" | "bigger";
